import { IMAGE_CONSTANTS } from "@constants/imageConstants";

export const TABLEPAGE_CONSTANTS = {
    TABLE: {
        IMAGE: {
            MENU_LINE: IMAGE_CONSTANTS.MENU_LINE,
            
        }
    }

};